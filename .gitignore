# Virtual Environment
venv/
.venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Environment variables
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Vendor / local-only AI tooling (install via Cursor plugins or locally; do not ship)
.cursor/skills/
.impeccable/
.opencode/
opencode.jsonc
.github/skills/
# gh-aw agentic sources (not compiled / not used — real Actions are *.yml only)
.github/workflows/*.md
.github/workflows/*.lock.yml

# Testing & Coverage
.pytest_cache/
.coverage
htmlcov/
coverage.xml
*.cover
.hypothesis/

# Build artifacts
dist/
build/
*.egg-info/
*.egg

# Jupyter
.ipynb_checkpoints/
*.ipynb

# OS files
.DS_Store
Thumbs.db

# Render (no local files needed)

# Local output
ranking_results/
*.json
!package.json
!package-lock.json
!tsconfig.json
!.cursor/environment.json
!frontend/static/rankings/.gitkeep
!frontend/static/rankings/**/*.json

# Security scan reports
bandit-report.json

# Cache directory
.cache/

# Precomputed rankings on API host (generated locally / by Actions)
static_rankings/

# AI Instructions (project-specific, not shared)
.github/copilot-instructions.md
