# Netlify configuration for SvelteKit Frontend
# This file is used by the SvelteKit adapter-netlify
# Note: Redirects must go in static/_redirects file, not here

[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"
