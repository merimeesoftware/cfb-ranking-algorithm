# Netlify configuration for SvelteKit Static Frontend
# Using adapter-static for simple static hosting

[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"
