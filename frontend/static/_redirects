# Cloudflare Pages: proxy /api/* to backend (same-origin, no VITE_API_URL needed)
# Update the host below when you move the API (Render → Cloudflare Containers, etc.)
/api/*  https://cfb-rankings-api.onrender.com/:splat  200
