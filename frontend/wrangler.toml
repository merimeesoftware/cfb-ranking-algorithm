name = "cfb-rankings-frontend"
compatibility_date = "2026-03-01"
pages_build_output_dir = "build"

# Pages deploys via Cloudflare Git integration (dashboard).
# Set API_ORIGIN per environment in the dashboard:
#   Production → https://cfb-rankings-api.<account>.workers.dev
#   Preview    → https://cfb-rankings-api-dev.<account>.workers.dev

[vars]
# Placeholder for local `wrangler pages dev`; override in Cloudflare dashboard.
API_ORIGIN = "http://localhost:5001"
