{
  "name": "cfb-ranking-algorithm",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "postinstall": "npm ci --prefix frontend && npm ci --prefix worker",
    "build": "npm ci --prefix frontend && npm run build --prefix frontend",
    "check": "npm run check --prefix frontend && npm run check --prefix worker",
    "deploy": "npm run build && wrangler deploy",
    "deploy:dev": "npm run build && wrangler deploy --env dev",
    "check:worker": "npm run check --prefix worker"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
